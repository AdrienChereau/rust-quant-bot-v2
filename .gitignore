# Rust
/target

# Secrets — NE JAMAIS committer
.env
.env.*
!.env.example
*.pem
*.key
# Hôtes de déploiement (IP privées) — copier hosts.env.example → hosts.env
deploy/hosts.env

# Python (scripts/derive_poly_creds.py)
.venv/

# État runtime + logs paper (state/log JSON : règle de sécurité — jamais commités)
/data/*
!/data/.gitkeep

# OS
.DS_Store

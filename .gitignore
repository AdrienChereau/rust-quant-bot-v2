# Rust build
/backend/target

# Secrets — ne JAMAIS committer
.env
**/.env

# Fichiers temporaires (écriture atomique de l'état, backups)
*.tmp
*.json.prev
*.before_restart_*.json

# OS
.DS_Store

# NB : backend/data/ est suivi VOLONTAIREMENT (état paper, trades, logs)
# pour permettre l'analyse des résultats. Aucun secret n'y figure.
